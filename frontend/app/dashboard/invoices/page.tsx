export default function DashboardInvoicesPage() {
  return (
    <div className="rounded-3xl border border-slate-200 bg-white p-10 shadow-sm">
      <p className="text-sm uppercase tracking-[0.2em] text-sky-600">
        Facturas
      </p>
      <h1 className="mt-3 text-3xl font-semibold text-slate-900">
        Modulo en preparacion
      </h1>
      <p className="mt-3 max-w-2xl text-slate-500">
        La base del sistema ya esta lista para conectar la facturacion sobre las ventas
        registradas. El siguiente bloque puede enfocarse en numero de factura, datos
        fiscales, PDF e integracion normativa si se requiere.
      </p>
    </div>
  );
}
